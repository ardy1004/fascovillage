// Since this is a static site without an API, we don't have Tanstack Query hooks for mutations.
// Form validation is purely client-side using React Hook Form and Zod as implemented in LeadFormSection.tsx
// This file is kept empty but created to satisfy the requested directory structure if needed elsewhere.
export {};
